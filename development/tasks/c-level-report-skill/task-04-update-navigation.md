---
task_id: 4
title: "Update CLAUDE.md navigation and create output directory"
type: task
agent: "doc-updater"
phase: 2
depends_on: [1, 2, 3]
status: "pending"
priority: "medium"
board: "[[c-level-report-skill/README]]"
files:
  - "CLAUDE.md"
  - ".claude/skills/CLAUDE.md"
  - "development/CLAUDE.md"
  - "development/C-level_reports/.gitkeep"
tags:
  - task
  - documentation
  - navigation
---

# Task 4: Update CLAUDE.md Navigation & Create Output Directory

## Assigned Agent: `doc-updater`

## Objective

Update all relevant CLAUDE.md navigation files to include the new `c-level-report` skill, and create the output directory `development/C-level_reports/`.

## Dependencies

- Tasks 1-3 must be complete (skill files must exist before documenting them)

## Files to Modify

### 1. Root `CLAUDE.md` — Skills Table

Add to the skills table in the "Skills" section:

```markdown
| `c-level-report` | `/c-level-report` | C-level executive report: gathers project metrics A-Z, generates rich report with KPIs, risk, roadmap. Saves to `development/C-level_reports/` |
```

### 2. `.claude/skills/CLAUDE.md` — Skill Inventory

Add the new skill to the inventory with:
- Name, command, description
- File paths
- Allowed tools
- Data sources

### 3. `development/CLAUDE.md` — Subdirectories Table

Add `C-level_reports/` to the subdirectories listing:

```markdown
| `C-level_reports/` | C-level executive reports generated by `/c-level-report` skill |
```

### 4. Create Output Directory

- Create `development/C-level_reports/.gitkeep` (ensures git tracks the empty directory)

## Acceptance Criteria

- [ ] Root `CLAUDE.md` skills table includes `c-level-report`
- [ ] `.claude/skills/CLAUDE.md` inventory updated with full skill details
- [ ] `development/CLAUDE.md` lists `C-level_reports/` directory
- [ ] `development/C-level_reports/` directory exists with `.gitkeep`
- [ ] All `<!-- last-updated -->` timestamps updated on modified CLAUDE.md files

## Estimated Complexity

**Low** — Small edits to existing navigation files + directory creation.
