---
type: daily-note
date: 2026-03-22
tags:
  - daily
---

# 2026-03-22 Sunday

## Human Notes

> Write your observations, decisions, and plans here.

## Agent Activity

> Auto-populated by agents. Do not edit below this line manually.

### Changes Made

- `agent/memory/redis_cache.py` — Fixed `get_cached()` glob bug (always returned None); added 24h TTL to working memory via atomic pipeline
- `agent/logging_middleware.py` — `log_api_call()` accepts optional `writer: LogBatchWriter` parameter
- `agent/server.py` — `LogBatchWriter` singleton lifecycle; `IntentRouter` registered with 7 handlers
- `agent/server_handlers.py` — New file: 7 async intent handler functions + `REASONING_LOOP_SENTINEL`
- `src/utils/exceptions.py` — Added `PermissionDenied(TradingPlatformError)` with code="permission_denied", http_status=403
- `agent/permissions/enforcement.py` — Imports `PermissionDenied` from `src.utils.exceptions` (removed local definition)
- `agent/tests/test_server_writer_wiring.py` — New file, 20 tests
- `agent/tests/test_server_handlers.py` — New file, 54 tests
- `agent/tests/test_redis_memory_cache.py` — 4 new `TestGetCached` tests; pipeline TTL verification
- `tests/unit/test_exceptions.py` — 8 new `PermissionDenied` tests
- `tests/unit/test_permission_enforcement.py` — 10 new tests

### Decisions

- `PermissionDenied` promoted from local exception to `TradingPlatformError` subclass in `src/utils/exceptions.py` — makes it auto-serializable by the global handler and consistent with platform error conventions
- `get_cached()` requires `agent_id` parameter — Redis does not support glob on `GET`; callers always know the agent_id in context

### Issues Found

- Redis `get_cached()` glob pattern `agent:memory:*:{memory_id}` was silently always returning None — root cause is that `GET` does not support glob patterns in Redis; required both keys to be known

### Changes Made (Phase 1 Branch + Phase 2 Independent)

- `agent/strategies/regime/labeler.py` — Added `_volume_ratio_series()` and `volume_ratio` as 6th classifier feature
- `agent/strategies/regime/classifier.py` — Updated `FEATURE_NAMES` to 6; added missing `_print_evaluation()`
- `tradeready-gym/tradeready_gym/rewards/composite.py` — New `CompositeReward` class (Sortino/PnL/activity/drawdown, weighted)
- `agent/strategies/rl/config.py` — Added `"composite"` reward type + 5 config fields
- `agent/strategies/rl/train.py` — `_build_reward()` composite case wired
- `agent/strategies/evolutionary/evolve.py` — `compute_composite_fitness()` with 5 factors + OOS Sharpe; dual IS/OOS battle loop
- `agent/strategies/evolutionary/config.py` — `oos_split_ratio` field; `fitness_fn` default → `composite`
- `agent/strategies/evolutionary/battle_runner.py` — `get_detailed_metrics()` for full 5-metric extraction
- `agent/strategies/risk/sizing.py` — `KellyFractionalSizer`, `HybridSizer`, `SizingMethod` enum
- `agent/strategies/risk/risk_agent.py` — `DrawdownProfile`, `DrawdownTier`, 3 presets (AGGRESSIVE/MODERATE/CONSERVATIVE)
- `agent/strategies/risk/veto.py` — `scale_factor` added to `VetoDecision`
- `agent/strategies/risk/middleware.py` — `_check_correlation()` gate (step 5); Pearson r on log-returns; fixed `SizingMethod` import bug
- `agent/strategies/ensemble/circuit_breaker.py` — New `StrategyCircuitBreaker` (3 triggers, Redis-backed, TTL auto-expiry)
- `agent/strategies/ensemble/run.py` — Circuit breaker wired into `EnsembleRunner`
- `agent/tools/sdk_tools.py` — 6 new tools (limit, stop-loss, take-profit, cancel, cancel-all, open-orders); `_serialize_order()` helper

### Decisions (Phase 1 Branch + Phase 2 Independent)

- `volume_ratio` as 6th regime feature — ADX/ATR alone miss institutional volume shifts; normalisation needed to handle varying volume regimes
- `CompositeReward` in `tradeready-gym/` (not inline) — keeps gymnasium env boundary clean; reusable across future environments
- OOS fitness in evolutionary GA uses held-out battle window (30% split) — prevents overfitting to in-sample period
- `DrawdownProfile` presets (`AGGRESSIVE`/`MODERATE`/`CONSERVATIVE`) — prevents misconfigured thresholds from creating dangerous step schedules
- `StrategyCircuitBreaker` Redis-backed with TTL — pause state survives restarts; no cron needed to reset
- Kelly/Hybrid sizing separate from `DynamicSizer` — different input requirements; callers choose based on data availability

### Issues Found (Phase 1 Branch + Phase 2 Independent)

- `middleware.py` imported `SizingMethod` before it was defined in `sizing.py` — fixed by moving import and adding to `__init__.py`
- `classifier.py` referenced `_print_evaluation()` that was never defined — fixed in Task 08
- Pearson r is near zero on linear synthetic price data — test fixtures must use shared random shocks, not linear sequences

### Changes Made (Tasks 21-22, 24, 26-27, 30, 32-37)

- `agent/strategies/risk/recovery.py` — New `RecoveryManager` (3-state FSM, ATR normalization, Redis persistence, 53 tests) — Task 21
- Security review Task 22: 0 CRITICAL, 0 HIGH; 2 MEDIUM deferred; all risk gates confirmed fail-closed
- `agent/tools/sdk_tools.py` — Added `get_ticker()` and `get_pnl()` tools (15 total); volume confirmation filter in SignalGenerator; confidence threshold 0.5→0.55; 35 new tests — Task 24
- `agent/trading/pair_selector.py` — New `PairSelector` (volume/spread filters, momentum tier, 1h Redis cache, 42 tests) — Task 26
- `agent/trading/ws_manager.py` — New `WSManager` (WS price buffer, order fills, REST fallback, 46 tests) — Task 27
- `agent/tasks.py` — Added `settle_agent_decisions` Celery beat task (every 5 min, 16 tests) — Task 30
- `agent/trading/journal.py` + `agent/memory/retrieval.py` — Memory-driven learning loop (episodic + procedural save, targeted retrieval, 29 tests) — Task 32
- `agent/tools/rest_tools.py` — 5 new REST tools (backtest compare/best/equity, decision analysis, risk profile; 24 tests) — Task 33
- `Frontend/src/components/battles/` — 9 new components, 3 routes, 2 hooks, 14 API functions, 15 types — Task 34
- `Frontend/src/components/agents/` — 4 new dashboard components (attribution, equity comparison, confidence histogram, trade monitor), 2 new hooks — Task 35
- `monitoring/prometheus.yml` — Fixed scrape config to include agent:8001; Grafana auto-provisioning confirmed — Task 36
- Performance audit Task 37: 2 HIGH (ContextBuilder client churn, WSManager subscribes all 600+ pairs), 3 MEDIUM, 1 LOW identified

### Decisions (Tasks 21-37)

- `RecoveryManager` uses ATR normalization (not fixed time delay) for HALT resumption — prevents resuming during volatility spikes
- Memory-driven learning uses targeted retrieval by type + tags (not semantic search) — procedural memories for symbol/regime are structurally tagged; semantic search would surface irrelevant memories
- `PairSelector` 1h cache (not per-tick) — 24h volume rankings are stable; per-tick refresh would hammer market API
- Battle frontend as standalone routes — battles involve multiple agents, needs independent creation/monitoring/replay UI
- `settle_agent_decisions` as async Celery task (not on trade fill) — decouples feedback loop from hot execution path

### Issues Found (Tasks 21-37)

- Prometheus scrape config only targeted :8000 (platform); agent metrics on :8001 were never scraped — fixed in Task 36
- `ContextBuilder.build()` creates new HTTP client on every call (no connection pooling) — HIGH perf issue flagged in Task 37 audit; not yet fixed
- `WSManager` subscribes to all 600+ pairs instead of filtered pair universe — HIGH perf issue; fix is to wire `PairSelector` output

### Changes Made (Final Session — All 37/37 Tasks Complete)

- `agent/strategies/drift.py` — New `DriftDetector` (Page-Hinkley test, log-return transforms, integrated into `TradingLoop._observe()`)
- `agent/strategies/retrain.py` — New `RetrainOrchestrator` (4 schedules: ensemble 8h, regime 7d, genome 7d, PPO 30d, A/B gate, 57 tests)
- `agent/strategies/ensemble/attribution.py` — New `AttributionLoader`, `AttributionResult` (reads 7-day Celery attribution, updates MetaLearner weights, auto-pauses negative-PnL strategies, 45 tests)
- `agent/strategies/walk_forward.py` — New walk-forward validation module (WFE, rolling windows, RL + evolutionary integrations, 94 tests)
- `agent/strategies/ensemble/meta_learner.py` — Dynamic weights: rolling Sharpe (deque maxlen=50), regime-conditional modifiers (TRENDING/MEAN_REVERTING/HIGH_VOL/LOW_VOL), `apply_attribution_weights()` (55 tests)
- `agent/strategies/ensemble/run.py` — `BacktestValidationReport`, `build_validation_report()` 5 acceptance criteria (40 tests)
- `scripts/e2e_provision_agents.py` — New: provisions 5 trading agents with distinct risk profiles for production deployment
- `monitoring/provisioning/datasources/prometheus.yml` — Grafana auto-provisioned datasource
- `monitoring/provisioning/dashboards/dashboards.yml` — Grafana dashboard loader
- `docker-compose.yml` — 3 new Grafana provisioning volume mounts
- `prometheus.yml` — `rule_files:` stanza + `agent:8001` scrape job
- `Frontend/src/components/battles/*.tsx` — 7 components finalized (BattleList, BattleDetail, BattleCreateDialog, BattleLeaderboard, BattleReplay, EquityCurveChart, AgentPerformanceCard)
- `Frontend/src/hooks/use-battles.ts`, `use-battle-results.ts` — 2 new battle hooks
- `Frontend/src/app/(dashboard)/battles/page.tsx`, `[id]/page.tsx` — Battle list + detail pages
- `development/context.md` — Updated Current State: all 37 tasks complete, final session summary added
- All CLAUDE.md files synced across agent/, agent/strategies/, agent/trading/, agent/tests/, scripts/, monitoring/, Frontend/

### Decisions (Final Session)

- `DriftDetector` uses Page-Hinkley test (not CUSUM) — simpler, single `delta` hyperparameter, better suited for streaming log-returns
- `RetrainOrchestrator` A/B gate uses `min_improvement=0.01` — 1% minimum Sharpe improvement required to deploy; prevents marginal regressions from replacing production model
- Grafana provisioning via volume mount (not manual import) — enables fully automated dashboard deployment on `docker compose up`

### Issues Found (Final Session)

- `monitoring/provisioning/` must mount to `/etc/grafana/provisioning/` (not `/var/lib/grafana/provisioning/`) — Grafana reads provisioning config from `/etc/grafana/provisioning/` at startup
- Walk-forward `asyncio.to_thread` wrapping for SB3 training is required — SB3 uses CPU-bound loops that block the event loop without threading

## Links

- Previous: [[2026-03-21]]
- Next: [[2026-03-23]]
- Context: [[context]]
