---
type: daily-note
date: 2026-04-02
tags:
  - daily
---

# 2026-04-02 Thursday

## Human Notes

> Write your observations, decisions, and plans here.

## Agent Activity

> Auto-populated by agents. Do not edit below this line manually.

### Changes Made

- `src/accounts/service.py` — BUG-001: register() auto-creates default agent via lazy AgentService import; AccountCredentials gained agent_id/agent_api_key fields. BUG-002: reset_account() rewritten to be fully agent-aware — fetches all non-archived agents, cancels orders/sessions at account level, re-creates per-agent Balance and TradingSession rows.
- `src/api/routes/auth.py` — BUG-001: POST /auth/register response now includes agent_id and agent_api_key fields.
- `src/api/schemas/auth.py` — BUG-001: RegisterResponse schema gained agent_id: UUID | None and agent_api_key: str | None optional fields.
- `src/api/routes/account.py` — BUG-017: /account/positions fetches real opened_at from Position table (removes 1970-01-01 epoch sentinel). Fixed asyncio.gather on shared DB session causing IllegalStateChangeError.
- `src/api/routes/battles.py` — BUG-003: _battle_to_response() checks SQLAlchemy inspect state before accessing participants relationship, preventing MissingGreenlet in async context.
- `src/api/routes/market.py` — BUG-012: GET /market/tickers symbols param is now optional; returns all tickers when omitted.
- `src/api/schemas/trading.py` — BUG-015: OrderRequest.price field now accepts stop_price as alias via AliasChoices.
- `src/database/repositories/battle_repo.py` — BUG-003: Removed local BattleNotFoundError; raises TradingPlatformError subclasses from src/utils/exceptions.py.
- `src/order_engine/engine.py` — BUG-011: _upsert_position() includes fee in buy cost basis (fee-inclusive avg_entry_price) and subtracts sell fee from realized_pnl.
- `src/risk/manager.py` — BUG-016: position_limit_exceeded rejection message now includes current/projected/max position values and total equity.
- `src/strategies/service.py` — BUG-005: create_strategy() catches pydantic.ValidationError and raises InputValidationError (HTTP 400) instead of unhandled 500.
- `src/tasks/celery_app.py` — BUG-018: Wrapped find_spec("agent.tasks") in try/except ModuleNotFoundError; fixes Celery startup crash when agent/ dir present but not installed.
- `alembic/versions/021_fix_cascade_delete_agent_fks.py` — BUG-004: New migration; adds ON DELETE CASCADE to 6 FK constraints on agent-scoped trading tables. Applied to production. Head: 020 → 021.
- `scripts/backfill_history.py` — Fixed dry-run memory leak (accumulated candles list) and candle double-counting (off-by-one on to_dt).
- `Frontend/src/lib/types.ts` — BattleLiveParticipant.rank: number → number | null (null until battle completes).
- `Frontend/src/lib/api-client.ts` — 401 JWT expiry now falls back to API key auth for battle endpoints.
- `Frontend/src/hooks/use-battles.ts` — Now accepts API key auth (not JWT-only).
- `Frontend/src/hooks/use-battle-results.ts` — Now accepts API key auth (not JWT-only).
- `development/qa-bugfix-plan.md` — Full QA plan with all 17 bugs, priorities, and fix details (new file).
- `development/reports/tester-guide.md` — Tester onboarding guide for running QA against production (new file).
- `development/tasks/qa-bugfix-sprint/` — 13 task files + README + run-tasks (new directory, all tasks DONE).

### Decisions

- Registration auto-creates a default agent: eliminates the two-step "register then create agent" flow. Agent creation failure is non-fatal (agent_id/agent_api_key will be null); caller can create an agent manually. The account-level api_key is NOT the trading key — use agent_api_key.
- BattleNotFoundError migrated to TradingPlatformError hierarchy: ensures global exception handler correctly serializes it and returns proper HTTP status code (was previously unrecognized).
- Migration 021 is additive (ALTER TABLE ADD/DROP CONSTRAINT only): safe for zero-downtime deployment; no data changes.

### Issues Found

- BUG-001 (P0): New accounts had zero balance immediately after registration — auto-agent creation was missing.
- BUG-003 (P0): Battle fetch/creation raised MissingGreenlet — SQLAlchemy lazy load attempted outside async session in _battle_to_response().
- BUG-005 (P0): POST /strategies returned 500 on invalid strategy definition — unhandled pydantic.ValidationError.
- BUG-004 (P1): DELETE /agents/{id} raised FK violations on 6 child tables — missing ON DELETE CASCADE.
- BUG-011 (P2): Win rate inflated in analytics — trading fees not included in realized_pnl calculation.
- BUG-017 (P2): All positions showed opened_at as 1970-01-01 — epoch sentinel was hard-coded instead of fetching from DB.

## Links

- Previous: [[2026-04-01]]
- Context: [[context]]
