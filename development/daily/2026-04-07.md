---
type: daily-note
date: 2026-04-07
tags:
  - daily
---

# 2026-04-07 Tuesday

## Human Notes

> Write your observations, decisions, and plans here.

## Agent Activity

> Auto-populated by agents. Do not edit below this line manually.

### Changes Made

- `src/api/schemas/battles.py` — Added typed `BattleLiveParticipantSchema` (13 fields); updated `BattleLiveResponse` with `elapsed_minutes`, `remaining_minutes`, `updated_at`. Replaces previous `dict[str, Any]` shape that caused frontend crash.
- `src/api/routes/battles.py` — Added elapsed/remaining time computation, `model_validate()` conversion, `sa_inspect` error handling to `GET /battles/{id}/live` handler.
- `src/battles/service.py` — `get_live_snapshot()` now returns all 13 participant fields including avatar_url, color, current_equity, roi_pct, total_pnl, total_trades, win_rate, live-computed rank, sharpe_ratio/max_drawdown_pct (null during live).
- `Frontend/src/components/battles/BattleDetail.tsx` — Fixed `!== null` to `!= null` (primary crash fix); added elapsed time badge.
- `Frontend/src/components/battles/AgentPerformanceCard.tsx` — Null-safe `total_trades` display.
- `Frontend/src/components/battles/BattleList.tsx` — Null-safe leader ROI display.
- `tests/integration/test_battle_endpoints.py` — Updated all mocks for new field names.
- `tests/integration/test_real_user_scenario_e2e.py` — Updated mocks for new field names.
- `src/backtesting/sandbox.py` — `BacktestSandbox` now records `stop_price` on `SandboxTrade` for stop-loss/take-profit fills (BT-02, BT-17).
- `src/backtesting/engine.py` — `_persist_results()` writes `stop_price` to `BacktestTrade` DB rows.
- `src/api/routes/backtest.py` — Fixed orphan detection prematurely marking new sessions as "failed"; fixed compare endpoint returning null metrics for cancelled sessions.
- `alembic/versions/022_add_stop_price_to_backtest_trades.py` — New migration: nullable `stop_price NUMERIC(20,8)` on `backtest_trades`. Head: 021 → 022.
- `tests/unit/test_backtest_sandbox.py` — Updated to verify `stop_price` on filled stop-loss/take-profit trades.
- `alembic/CLAUDE.md` — Added migration 022, updated head from 021 to 022, updated timestamp to 2026-04-07.
- `src/backtesting/CLAUDE.md` — Added stop_price fix to Recent Changes, updated timestamp.
- `development/context.md` — Pruned from 931 to ~409 lines: summarized 2026-03-21/22 detailed entries, added backtest fixes to 2026-04-07 session, updated DB head to 022.

### Decisions

- Introduced typed `BattleLiveParticipantSchema` instead of `dict[str, Any]` to enforce the 13-field contract at the schema level — prevents future field-name drift between backend and frontend.
- `sharpe_ratio` and `max_drawdown_pct` are intentionally null during a live battle (rolling Sharpe with few data points would be misleading); they populate only in the results endpoint after completion.

### Issues Found

- Root cause of crash: `!== null` (strict) doesn't guard `undefined`; the field arrived as `undefined` due to name mismatch, not `null`. Loose `!= null` is the correct guard when API fields could be missing entirely.
- The `dict[str, Any]` pattern in schemas gives zero protection against backend/frontend field-name drift. Any live endpoint returning participant data should use a typed schema.
- Orphan detection in backtest routes was checking session age from `created_at` without first verifying `is_active()` — caused race condition where newly-created sessions were prematurely marked "failed".
- `stop_price` on `BacktestTrade` was missing because the sandbox computed fills but never stored the trigger price; required threading through sandbox → engine → DB + migration.

---

### Changes Made (V.0.0.3 Context Sync)

- `src/webhooks/CLAUDE.md` — Created new CLAUDE.md for webhooks package (new module in V.0.0.3).
- `CLAUDE.md` (root) — Added `src/webhooks/CLAUDE.md` row to backend module index.
- `src/backtesting/CLAUDE.md` — Added `step_batch_fast()`, `BatchStepResult`, `fee_rate` config; updated Recent Changes.
- `src/metrics/CLAUDE.md` — Added `deflated_sharpe.py` to Key Files table; updated Recent Changes.
- `src/api/routes/CLAUDE.md` — Added `indicators.py`, `metrics.py`, `webhooks.py` route files; new endpoint tables for indicators, metrics, webhooks, strategy compare, backtest fast-batch.
- `src/api/schemas/CLAUDE.md` — Added 5 new schema files to Key Files table; updated Recent Changes.
- `src/tasks/CLAUDE.md` — Added `webhook_tasks.py` to Key Files; updated Recent Changes.
- `tests/unit/CLAUDE.md` — Added 8 new test files; updated test count (1734 → ~2280+, files 87 → 95+).
- `tests/integration/CLAUDE.md` — Added 4 new test files; updated total (554 → ~644, files 26 → 30).
- `development/context.md` — Added V.0.0.3 Recent Activity entry; updated Current State; updated What's Built table; updated migration head to 023; updated test totals to 5,132+.
- `development/CLAUDE.md` — Added V.0.0.3 milestone to timeline; updated Recent Changes.

### Decisions (V.0.0.3 Context Sync)

- Created `src/webhooks/CLAUDE.md` as a new package-level file since webhooks is a distinct domain (not a sub-module of tasks or api).
- Deflated Sharpe placed in `src/metrics/` (not `agent/strategies/`) because it is a general-purpose statistical function used by both the REST metrics API and the strategy search system.
- `step_batch_fast()` documented as trading snapshot fidelity for speed — skips per-step capture, designed for parameter sweeps.

## Links

- Previous: [[2026-04-06]]
- Context: [[context]]
