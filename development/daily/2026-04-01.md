---
type: daily-note
date: 2026-04-01
tags:
  - daily
---

# 2026-04-01 Wednesday

## Human Notes

> Write your observations, decisions, and plans here.

## Agent Activity

> Auto-populated by agents. Do not edit below this line manually.

### Changes Made

- `pyproject.toml` — Added 2 mypy per-module overrides for agent.strategies.rl and src.mcp.tools to suppress SB3/MCP stub warnings
- `requirements.txt` — Added numpy>=1.26 (required by src/strategies/, was missing from CI)
- `tests/unit/test_ab_testing.py` — Added pytest.importorskip("pandas") to skip when ML deps absent
- `tests/unit/test_strategy_manager.py` — Same pandas importorskip guard
- `tests/unit/test_agent_tools.py` — Added pytest.importorskip("agentexchange") guard
- `agent/strategies/rl/runner.py` — Fixed mypy type-ignore comments for SB3 BaseCallback stubs
- `.github/workflows/deploy.yml` — Major rewrite: reads DB creds from .env, 30m SSH timeout, excludes time-series tables from backup, stash+reset before pull, starts infra before alembic, uses docker compose run --rm
- `.github/workflows/test.yml` — Restricted to main branch only
- `docker-compose.yml` — Removed Redis --requirepass (internal network, no host port exposure)
- `.env.example` — Removed REDIS_PASSWORD, simplified REDIS_URL
- `alembic/versions/012_agent_scoped_unique_constraints.py` — Fixed: drop constraint before index (PostgreSQL requirement)
- `src/accounts/service.py` — Removed TradingSession creation from register(); TradingSession.agent_id is NOT NULL but registration has no agent_id — was causing every registration to fail
- `src/config.py` — Added https://tradeready.io and https://www.tradeready.io to default CORS_ORIGINS
- `development/deployment-fix-plan.md` — Deployment fix planning doc (archived)

### Decisions

- Redis password removed from default config: Redis is internal-network-only (no host port), so --requirepass adds complexity with no security benefit
- test.yml restricted to main branch: feature branch CI was consuming quota; production gate on main is sufficient
- docker compose run --rm preferred over exec for alembic in deploy: exec requires a running container; run --rm works regardless

### Issues Found

- **Registration always failed in production**: register() created TradingSession with agent_id=NULL but column is NOT NULL; IntegrityError was misclassified as DuplicateAccountError, masking the real cause
- **Migration 012 failed on redeploy**: DROP INDEX on a constraint-backing index raises ERROR; must DROP CONSTRAINT first

### C-Level Deployment & Data Continuity Analysis (later session)

**Changes Made**
- `development/C-level_reports/report-2026-04-01-deployment-analysis.md` — New C-level report: comprehensive analysis of CI/CD pipeline, migration safety, candle data continuity during deploys, data gap detection/recovery status, backup strategy, and monitoring coverage

**Decisions**
- No code changes made; this was a research and reporting task only

**Issues Found**
- Scheduled cron backup does not exist yet — pre-deploy pg_dump is the only automated backup; identified as an outstanding operational gap
- Data gap-fill task (designed in `development/gap_fill_implementation_plan.md`) has not been deployed to production; gaps can accumulate during rolling restarts without automatic recovery

## Links

- Previous: [[2026-03-31]]
- Next: [[2026-04-02]]
- Context: [[context]]
